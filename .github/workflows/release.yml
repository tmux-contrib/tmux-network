name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@6.0.2
      - uses: googleapis/release-please-action@4.4.0
        with:
          config-file: .github/config/release-please-config.json
          manifest-file: .github/config/release-please-manifest.json
